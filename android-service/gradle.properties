# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Enable Gradle Build Cache for faster builds
org.gradle.caching=true

# Enable configuration cache for faster configuration
org.gradle.configuration-cache=true

# Enable file system watching for faster incremental builds
org.gradle.vfs.watch=true

# Android Gradle Plugin settings
android.enableJetifier=true
android.suppressUnsupportedCompileSdk=34

# REMOVED: android.bundle.enableUncompressedNativeLibs=false
# This option was deprecated and removed in AGP 8.1+
# Native libs are now uncompressed by default for better performance

# R8 optimization settings
android.enableR8.fullMode=true

# Build performance optimizations
org.gradle.daemon=true
org.gradle.workers.max=4

# Signing settings (for release builds)
# These should be set as environment variables or in local.properties
# KEYSTORE_PASSWORD=your_keystore_password
# KEY_PASSWORD=your_key_password
# KEY_ALIAS=snappy-web-agent
# STORE_FILE=release.keystore

# Native build settings
android.native.useGradleBuildCache=true

# Experimental features (compatible with AGP 8.2+)
android.experimental.enableNewResourceShrinker=true

# AGP 8.2+ compatibility settings
android.defaults.buildfeatures.buildconfig=true
android.enableAdditionalTestOutput=false

# Disable warnings for deprecated features
android.suppressUnsupportedOptionWarnings=true